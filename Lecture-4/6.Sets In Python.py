#set is the collection of the unordered items
#each elements in the set must be unique & immutable

#sets ke andar list or dictionaries use nahi ho sakte

nums = { 1, 2, 3, 4, 2, 2, "hello",  "world", "world"} #duplicated value are ignored, Sets is Unordered
print(nums)
print(type(nums))
print(len(nums)) #duplicate value not be count in sets

